node_modules/
.venv/
.DS_Store
*.log
.idea/
.vscode/
.worktrees/

# Build artifacts (generated from src/)
leaki.html
www/index.html

# Capacitor native build artifacts (keep android/ source, ignore local caches)
android/.gradle/
android/app/build/
android/build/
android/captures/
android/local.properties
android/*.iml
android/.idea/

# Local sync snapshots (pairing keys hashed)
data/

# Cloudflare named tunnel (contains tunnel UUID / path to credentials)
cloudflared.yml

dist/
*.apk
*.aab
*.keystore

# Local Android Studio installers (multi-GB)
android-studio*.tar
android-studio*.tar.gz
*.Zone.Identifier
